<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DidILockMy.Views.BottomNavigation"
             HeightRequest="55"
             MinimumHeightRequest="55"
             BackgroundColor="White">
  <ContentView.Content>
        <RelativeLayout>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="55"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button x:Name="notificationBtn" Text="Not" Grid.Column="0" Grid.Row="0"/>
                <Button x:Name="editBtn" Text="Edit" Grid.Column="1" Grid.Row="0"/>
                <Button x:Name="deleteBtn" Text="Delete" Grid.Column="2" Grid.Row="0"/>
                <Button x:Name="settingsBtn" Text="Setting" Grid.Column="3" Grid.Row="0"/>
            </Grid>
        </RelativeLayout>
    </ContentView.Content>
</ContentView>
